import XCTest
@testable import FiniteEnum_module

final class FiniteEnum_moduleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FiniteEnum_module().text, "Hello, World!")
    }
}
